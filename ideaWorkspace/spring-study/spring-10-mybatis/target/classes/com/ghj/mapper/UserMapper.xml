<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ghj.mapper.UserMapper">
    <select id="select" resultType="com.ghj.pojo.User">
        select * from malajava.user
    </select>

    <insert id="insert" parameterType="com.ghj.pojo.User">
        insert into malajava.user (id, name, age, local_date_time) values (#{id}, #{name}, #{age}, #{local_date_time});
    </insert>
</mapper>