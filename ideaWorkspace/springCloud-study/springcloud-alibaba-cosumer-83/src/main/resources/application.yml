server:
  port: 83
spring:
  application:
    name: nacos-order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

server-url:
  nacos-payment-server: http://nacos-payment-provider

management:
  endpoints:
    web:
      exposure:
        include: '*'
